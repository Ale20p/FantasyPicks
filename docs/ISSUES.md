1) (Only fixed for when it's FantasyPros and Sleeper) When the user clicks on more than one source, some players' data/rankings are lost. So when the user only clicks on FantasyPros as the source it results in perfect results. But when the user clicks on FantasyPros and Sleeper as an example, then some players' rankings go missing in both or multiple columns.Let's create a Normalization Layer outlined in the ARCHITECTURE.md file that will fix this issue.

2) Yahoo rankings aren't being scraped correctly or at all. We need to fix this issue.
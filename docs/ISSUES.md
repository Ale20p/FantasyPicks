# Ignore These unless specified that they are targetted for resolution. 
1) Yahoo and ESPN rankings aren't being scraped correctly or at all. We need to fix this issue. When trying to read the site, it seems that AI gets thrown into an endless reading loop.

2) Defense and Kicker positions aren't being displayed for Sleeper.